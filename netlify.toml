[build]
  command   = "CI=false npm run build"
  publish   = "build"
  functions = "netlify/functions"

[functions."sync-scores"]
  schedule = "* * * * *"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
